{
  "extends": "@tsconfig/svelte/tsconfig.json", // Estende as configurações do TypeScript para Svelte
  "compilerOptions": {
    "target": "es2017", // Define a versão do ECMAScript para a qual o código será compilado
    // "verbatimModuleSyntax": true, // Use isso em vez de opções obsoletas
    "ignoreDeprecations": "5.0"  // Suprime avisos de depreciação a partir da versão 5.0
  },
  "include": ["src/**/*"], // Inclui todos os arquivos dentro da pasta 'src' e suas subpastas
  "exclude": ["node_modules/*", "__sapper__/*", "public/*"] // Exclui as pastas 'node_modules', '__sapper__' e 'public' da compilação
}